Logical Operators

 When we need to test for two or more  conditions we use LOGICAL OPERATORS
      and or not
      &&  || !

  boolean cold=true;
  boolean rain=true;

   AND -> &&
  true && true= true
  true && false= false
  false && true=false
  false && false=false
  if (cold && rain)  {--> FALSE
  SOUT ("I WILL STAY AT HOME")
  }
    OR-> ||
      true || true= true
      true || false= TRUE
      false || true=TRUE
      false || false=false

      IF ( COLD || RAIN ){
      }




      -----------------------------

      Loop - repeat block of code based on the condition
      4 Loops in Java:
     Use While you don't know how many times you will repeat the code
      while
      do while

     Use FOR you don't know in advanced how many times you will repeat the code
       for
      for each/advanced for loop/enhanced for loop
